import { MyCustomValidators } from './my-custom-validators';

describe('MyCustomValidators', () => {
  it('should create an instance', () => {
    expect(new MyCustomValidators()).toBeTruthy();
  });
});
